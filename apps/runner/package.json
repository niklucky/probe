{
  "name": "@probe/runner",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@probe/ai": "*",
    "@probe/db": "*",
    "@probe/shared": "*",
    "minio": "^8.0.2",
    "prettier": "^3.4.2",
    "typescript": "^5.6.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@probe/typescript-config": "*",
    "@types/bun": "latest"
  }
}
