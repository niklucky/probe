{
  "name": "@probe/runner",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@probe/db": "*",
    "minio": "^8.0.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@probe/typescript-config": "*",
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
